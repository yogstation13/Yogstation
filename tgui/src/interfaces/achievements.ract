<ui-display>
	{{#each data.achievements}}
		<ui-section label='{{name}}'>
			{{desc}}<br>
			{{#if unlocked}}
				<span class='good'>Unlocked</span>
			{{else}}
				<span class='bad'>locked</span>
			{{/if}}
		</ui-section>
	{{/each}}
</ui-display>
