<ui-display>
	{{#each data.achievements}}
		<ui-section label='{{name}}'>
			{{desc}}
			{{unlocked ? "<span color='green'>Unlocked</span>" : "<span color='red'>locked</span>"}}
		</ui-section>
	{{/each}}
</ui-display>
