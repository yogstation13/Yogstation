<ui-display title='Achievements'>
	{{#each data.achievements}}
		<ui-section label='{{name}}' candystripe right>
			<span>{{desc}}</span>
			{{unlocked ? "<span color='green'>Unlocked</span>" : "<span color='red'>locked</span>"}}

		</ui-section>
	{{/each}}

</ui-display>
