<ui-display>
	{{#each data.achievements}}
		<ui-section label='{{name}}'>
			{{desc}}<br>
			{{#if unlocked}}
				<th style="color: '#00FF00';">Unlocked</th>
			{{else}}
				<th style="color: '#c51e1e';">locked</th>
			{{/if}}
		</ui-section>
	{{/each}}
</ui-display>
